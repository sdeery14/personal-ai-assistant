# Custom MLflow image with PostgreSQL support
FROM python:3.12-slim

# Install MLflow 3.8.1 and PostgreSQL client library
RUN pip install --no-cache-dir mlflow==3.8.1 psycopg2-binary

# Expose MLflow port
EXPOSE 5000
